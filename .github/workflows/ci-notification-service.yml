# CI: notification-service - build, test (80% coverage), SonarCloud, build image, push GHCR
# Testcontainers (RabbitMQ) requer Docker no runner - ubuntu-latest has it
name: CI Notification Service

on:
  push:
    branches: [main]
    paths:
      - 'notification-service/**'
      - 'pom.xml'
      - '.github/workflows/ci-notification-service.yml'
  pull_request:
    branches: [main]
    paths:
      - 'notification-service/**'
      - 'pom.xml'
      - '.github/workflows/ci-notification-service.yml'

env:
  JAVA_VERSION: '21'
  MAVEN_OPTS: -Dhttp.keepAlive=false -Dmaven.wagon.http.pool=false

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Build, test and SonarCloud
        run: |
          cd notification-service && mvn -B verify sonar:sonar \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.organization=pos-tech-fiap-group \
            -Dsonar.coverage.exclusions="src/main/java/**/NotificationServiceApplication.java,src/main/java/**/config/**/*.java,src/main/java/**/dto/**/*.java,src/main/java/**/exception/**/*.java,src/main/java/**/exceptionhandler/**/*.java,src/main/java/**/ports/**/*.java,src/main/java/**/domain/enums/**/*.java"
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_VIDEO_PROCESSOR }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set GHCR image owner (lowercase)
        if: github.event_name != 'pull_request'
        run: echo "REPO_OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: notification-service/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.REPO_OWNER_LC }}/video-processor-notification-service:${{ github.sha }}
            ghcr.io/${{ env.REPO_OWNER_LC }}/video-processor-notification-service:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
